# DeepScalper Pilot Test Configuration (Single BDQ)
# Goal: Verify logic, connections, and initial learning.
# Duration: ~15 mins

data:
  file_path: "data/btc_lob_jan2023.parquet"
  ticker: "BTCUSDT"
  train_start_date: "2023-01-01"
  train_end_date: "2023-01-07"   # 1 week data
  val_start_date: "2023-01-07"
  val_end_date: "2023-01-08"
  test_start_date: "2023-01-08"
  test_end_date: "2023-01-09"

features:
  micro_features: ["micro"]
  macro_features: ["macro"]
  private_features: ["position", "balance"]

env:
  symbol: "BTCUSDT"
  initial_balance: 100000.0
  transaction_fee: 0.00005 
  num_envs: 1            # Single Env for Windows Pilot Stability
  window_size: 50
  
  reward:
    profit_weight: 1.0
    hindsight_weight: 0.1
    hindsight_horizon: 180
    volatility_horizon: 100
    
  action:
    direction_bins: 3 
    price_bins: 5     
    volume_bins: 5    
    max_position: 5.0

network:
  micro_config:
    input_size: 20
    private_input_size: 2
    hidden_size: 128
    rnn_type: "LSTM"
  macro_config:
    input_size: 11
    hidden_sizes: [256, 128] 

agents:
  bdq:
    learning_rate: 0.0001
    gamma: 0.99
    auxiliary_weight: 1.0
    update_interval: 2.0 
    batch_size: 128
    buffer_size: 50000   
    target_update_freq: 1000
    checkpoint_interval: 10000 
    learning_starts: 1000
    epsilon_start: 1.0
    epsilon_end: 0.05
    epsilon_decay: 0.999    # Faster decay for pilot to see some exploitation

training:
  total_timesteps: 100000   # 100k steps (Pilot Scale)
  log_interval: 500
  
  torch_compile: false      # Skip compilation overhead
  use_amp: true         
  use_shm: false
  
wandb:
  project: "FinRL-Pro-DS"
  entity: "bigcan-chiwin-technology"
  mode: "online"
  tags: ["Pilot", "SingleBDQ", "VerifyingLogic"]
